"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : new P(function (resolve) { resolve(result.value); }).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
const path = require("path");
const os = require("os");
const ava_1 = require("ava");
const fs_1 = require("./fs");
const test = ava_1.default;
test.beforeEach((t) => __awaiter(this, void 0, void 0, function* () {
    t.context.directory = yield fs_1.mkdtemp(path.join(os.tmpdir(), 'fs-test-'));
}));
[
    { input: '/foo' },
    { input: '/foo/bar/baz' },
].forEach(({ input }) => {
    test(`mkdirp(${JSON.stringify(input)})`, (t) => __awaiter(this, void 0, void 0, function* () {
        const absoluteInput = path.join(t.context.directory, input);
        yield fs_1.mkdirp(absoluteInput);
        const stats = yield fs_1.stat(absoluteInput);
        t.true(stats.isDirectory());
    }));
});
