import * as globby from 'globby';
export interface IHexoContext {
    log: {
        info: (...args: Array<string | number | {}>) => void;
    };
    public_dir: string;
}
export interface IHexoDeployment {
    region: string;
    prefix: string;
    bucket: string;
    glob: globby.GlobbyOptions;
    test: string;
}
declare global {
    const hexo: {
        extend: {
            deployer: {
                register: (name: string, deployer: (this: IHexoContext, deploy: IHexoDeployment) => Promise<void>) => Promise<void>;
            };
        };
    };
}
